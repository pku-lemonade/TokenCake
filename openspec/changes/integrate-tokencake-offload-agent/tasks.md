# Implementation Tasks

## 1. Freeze Sources And Environments

- [x] 1.1 Record target/source/`vllm-note` commits, branches and worktree status, paper/source-document references, the transferred source experiment-helper manifest and hashes, model identity, dataset path and SHA-256, Mooncake package/wheel/config commits, GPU UUIDs, and NUMA CPU sets in a machine-readable provenance artifact; verify every recorded path/hash against a read-only command and identify `vllm-note` as reference-only.
- [x] 1.2 Create the target repo-local `.venv` with `uv` from the accepted `mooncake_agent`/Torch 2.11-compatible interpreter and package stack without modifying the original environment, install target vLLM and focused test/lint dependencies without using system Python or bare pip, install pre-commit hooks, and verify `.venv/bin/python` imports the target checkout, Mooncake 0.3.8, and the expected CUDA devices.
- [x] 1.3 Create an isolated repo-local source environment for exact `../vllm_agent` HEAD with its compatible Torch 2.6 stack, and verify its environment imports the unchanged source checkout without changing source git status.
- [x] 1.4 Materialize an immutable official-v0.22 baseline checkout/worktree at the recorded upstream commit and verify its scheduler, TokenCake configuration, and KV-offload settings are absent for baseline launches.

## 2. Add Structured Configuration And Request Metadata

- [x] 2.1 Add a compact `vllm/tokencake/` package and strict immutable parser for the accepted scheduling/offload schema and defaults; verify focused tests cover absent config, both independent switches, offload-only fixed `first_fit`, unknown keys, types/ranges, disabled-section tuning, watermark relationships, and no legacy environment/CLI activation.
- [x] 2.2 Validate TokenCake offload prerequisites and normalize the connector to the TokenCake subclass only when TokenCake offload is enabled; preserve standard native connector selection when TokenCake is absent, both switches are disabled, or scheduling alone is enabled; verify startup tests cover those combinations, CPU capacity, concrete CPU-manager compatibility, backend/connector mismatch, rejection of the simple-offload fallback, and `data_parallel_size > 1` failures.
- [x] 2.3 Broaden completion, chat, and responses `vllm_xargs` values to recursive JSON without changing other extension semantics; verify protocol tests accept nested JSON and retain existing scalar/list cases.
- [x] 2.4 Validate recognized `vllm_xargs["tokencake"]` fields before engine admission, require matching `tc-<uuid4-hex>` input IDs and exactly one expanded EngineCore request, apply neutral optional defaults, and preserve/ignore unknown nested metadata; verify valid, absent, malformed, mismatched, partial, extensible, annotated fan-out rejection, and ordinary fan-out cases at the OpenAI request boundary.

## 3. Implement Lifecycle Events And Bounded State

- [x] 3.1 Implement the minimal lifecycle state keyed only by explicit lifecycle UUID, with independent generation/stall facts and a detached per-group ordered hash/block/native-request-context snapshot captured before request cleanup; when start precedes completion, set one post-finish evaluation after capture and consume it only after native block release; when finish follows start but precedes completion, terminalize with one duration update and prevent later completion from capturing or reactivating state; make the first terminal transition authoritative; reject a duplicate generation association through the native request-error path without a synchronous reservation RPC; terminalize pre-completion abort/error without duration learning only when still live while preserving native `PREEMPTED` requeue; verify tests cover completion-before-start, start-finish-completion, start-finish-abort/error, start-before-completion as the final model request, one-shot rejection without spin, abort/error before and after start, preemption, stale block revalidation, retry isolation, duplicate-core association without overwrite/merge, no retained `Request`, and no GPU-block pinning.
- [x] 3.2 Implement fixed lazy expiry for the 60-second attach window, derived active safety deadline, predicted ownership release, and 60-second terminal tombstone, including pre-completion abort/error tombstones; verify deterministic-clock tests cover every boundary, late finish after abort/error, and cleanup that never evicts native CPU/GPU cache entries.
- [x] 3.3 Implement the four-case duration prediction rule, server-side monotonic duration measurement, a private 4096-entry LRU for shareable metadata keys, and lifecycle-local fallback samples; verify estimate-only, history-only, blended, default, LRU eviction, anonymous cleanup, duplicate, conflict, expiry, and reset-persistence cases.
- [x] 3.4 Add generic typed EngineCore utility handling for lifecycle events and wait for the core-applied acknowledgement; verify tests prove enqueue-only success is impossible, distinguish job registration from async completion, and preserve start-before-tool-sleep and finish-before-successor barriers.
- [x] 3.5 Add the authenticated `POST /v1/tokencake/events` route and strict start/finish models; verify endpoint tests cover default kind/duration, live and tombstone duplicate precedence, 200 applied/duplicate/late-finish, 400 schema, 401 authentication, 404 unknown, 409 conflict, 503 disabled/unavailable, and 500 internal failure without raw exception leakage.
- [x] 3.6 Integrate TokenCake cleanup with existing reset semantics and no new reset route; verify local-only invalid snapshot cleanup, successful external reset ordering, per-component refusal without contradictory cleanup or invented rollback, active-transfer fencing, tombstones, and persistent bounded EWMA/metrics behavior.
- [x] 3.7 Add bounded TokenCake lifecycle/decision/saved-block/scheduling metrics while retaining native connector metrics; verify metric tests contain only bounded labels and no request, agent, prompt, or lifecycle values.
- [x] 3.8 Verify routing tests return 404 for every excluded legacy `/v1/mcp*` path and that existing `/health`, `/metrics`, and `/reset_prefix_cache` behavior remains available.

## 4. Port Latest Agent Scheduling With Narrow Scheduler Changes

- [x] 4.1 Port the effective latest-source request score and per-agent importance calculations into the compact TokenCake scheduling controller, excluding unreachable/dead source code; verify golden tests compare representative source and migrated score/critical-type results.
- [x] 4.2 Port adaptive reserve sizing, partitioning, annotated borrowing, deferral/preemption history, and per-step accounting using v0.22 multi-cache-group allocation demand; verify tests cover low/high pressure bounds, important waiting owners, annotated idle borrowing, ordinary shared-capacity accounting without borrowing, allocation failure, commit, abort, finish, and no double release.
- [x] 4.3 Add a once-per-step frozen score snapshot and exact candidate selection over native waiting/skipped queues directly from `scheduler.py`; after native blocked/LoRA handling, reorder only the contiguous TokenCake prefix before the first ordinary barrier; verify stable removal, native tuple tie-break, same-queue and cross-queue three-item barriers under FCFS/priority, mixed ordinary traffic, and all-ordinary native fallback.
- [x] 4.4 Add the annotated-only 256-token prefill cap after native clamps under the exact latest-source backlog/80%-usage conditions, plus pre-allocation admission, successful-allocation commit, and deferral branches at the existing running/waiting scheduler points; verify native allocation constraints remain authoritative, ordinary/disabled requests bypass the TokenCake cap, and reservation denial leaves a request pending.
- [x] 4.5 Replace only native victim selection and retain the allocation-failure branch's complete rollback plus `_preempt_request()`; verify both unscheduled and already-scheduled victims, including token budget, new blocks, speculative tokens, encoder inputs, request index, `PREEMPTED` requeue/recompute, absence of terminal `FINISHED_PREEMPTED`, and exact reservation release.
- [x] 4.6 Verify disabled scheduling keeps native queue construction, `pop_request()` complexity/path, priority meaning, scheduling order, allocation, and preemption semantics; assert no scheduling controller, score computation, or TokenCake queue scan is active, and assert the connector subclass is absent when TokenCake is absent/both switches are disabled but remains independently selectable for offload-only mode, without a null-object or general hook framework.

## 5. Extend Native CPU Offload In Phase 1

- [x] 5.1 Add a protected scheduler-construction hook to `OffloadingConnector` and a protected early-defer predicate to `OffloadingConnectorScheduler` before store progress advances; verify their native defaults preserve exact behavior/metadata under existing connector tests, the subclass preserves ordinary-request automatic store/load metadata and `next_stored_block_idx`, and only an annotated deferred request leaves that index unconsumed.
- [x] 5.2 Register a lightweight TokenCake connector under `vllm/tokencake/offloading.py` that reuses the native worker, CPU manager/LRU, async job lifecycle, HMA support, completion fences, and stats; add only concrete CPU-manager `retain()`/`release()` operations over the existing eviction refcount, without changing the generic manager interface or adding a manager ownership table; verify absent/not-ready/ready keys, stacked native/lifecycle references, LRU/ARC eviction, exact decrement, and construction tests.
- [x] 5.3 Implement latest-source eligibility, pressure, waiting-demand, utility, transfer-time, eviction-window, maximum-relief, threshold, temporal-selection, ancestor-reachability, and rejection-backoff decisions over Phase 1's detached lifecycle snapshot; verify focused tests cover every accepted/rejected reason, continuous-prefix reachability, selected-block bound, and the fact that preservation does not claim to add GPU capacity.
- [x] 5.4 Before new block allocation, consume any post-finish marker after native block release and build selected snapshot blocks with native manager preparation, the shared job-ID space, `TransferJob` worker metadata, immediate fencing of every detached GPU source, and a detached completion map; keep each lifecycle's actually retained key set outside the manager, retain already-ready keys immediately, and retain newly stored keys only after successful completion while ownership remains active; force every first-publication step for unpublished detached jobs to be exclusive connector-only before any later model step submits/overlaps that D2H; verify event-handler and scheduler registration with runnable model work, completion-before-start, start-before-completion with an otherwise idle engine, later-waiting-demand selection, no retained finished `Request`, ordinary-result delegation, finish/expiry-before-completion, multiple-lifecycle/native-load refcounts, exact-once release, idle publication, attempted same-step source reuse, native one-step store deferral, following-model-step D2H overlap, completion/failure, and no spin for an active lifecycle without a job.
- [x] 5.5 Preserve native demand-driven H2D and pending-job source-reuse fencing without importing the old block pool, swap maps, related-block graph, model-runner transfer events, upload reservations/debt, or waiting H2D prefetch; verify continuous CPU prefix hit, miss/recompute, completion ordering, finish/release during in-flight D2H, preemption, reset, code-search exclusions, and native fail-closed behavior for post-ack D2H/H2D failure.
- [x] 5.6 Add one bounded, read-only BlockPool frontier observation for Phase-1 attribution and instrument snapshot survival with bounded counters; verify the observation cannot affect Phase-1 selection and the evidence can distinguish policy rejection, stale snapshot, a snapshot-external hashed frontier entry, fence wait, and transfer failure without request-specific metric labels.

## 6. Adapt The Existing Launcher And Benchmark Driver

- [x] 6.1 Store a minimal target-owned patch that materializes the adapted launcher from exact source commit `7a608a4e53ea990b2540c93b4d28cb795b905109` in a disposable checkout/archive, reusing only the transferred source experiment helpers needed by the accepted workflow rather than bulk-copying them into runtime code; verify the source worktree remains clean and the materialized launcher/helper hashes are reproducible.
- [x] 6.2 Generate a fresh `tc-<uuid4-hex>` inside every target retry attempt and always use it as the top-level request ID; use the same nested ID for agent-only/offload-agent and the same event ID only for offload-agent; map only accepted metadata names and make enabled event POSTs raise on non-2xx; verify per-mode request/event bodies, at-most-four attempts, prompt halving, DAG/tool timing, and new IDs per retry.
- [x] 6.3 Convert old mode settings to structured `additional_config["tokencake"]`, omit nested TokenCake metadata and events for native baseline, retain scheduling metadata but disable events for target agent-only, enable both for target offload-agent, preserve tool sleep in every mode, and leave latest-old protocol runs untouched; verify rendered commands and request bodies for all required modes.
- [x] 6.4 Update the benchmark driver to use a fresh server per case, fixed GPU UUID/NUMA CPU affinity, two independent concurrent queues, high-to-low QPS order, the repo-local uv environment derived from the current Mooncake runtime, and no smoke/truncation options; verify a dry planning command emits exactly the accepted matrix and ordering without launching a model server.
- [x] 6.5 Persist commands, resolved config, code/environment/model/dataset/patch hashes, GPU/process/thermal samples, peer case, raw output, `/metrics`, logs, correctness, exclusion reasons, retries, prompt truncations, and gate inputs; verify a synthetic completed case can be audited from artifacts alone.
- [x] 6.6 Implement full-DAG qualification, third-party GPU-process invalidation, old-work-equivalence handling, strict per-QPS gates, three-percentage-point repetition triggers, affected-pair-only reruns, implementation-phase case identity, shared-case reuse with a three-launch maximum including exclusions, unresolved-budget reporting, and median evaluation; verify deterministic synthetic reports exercise every pass, fail, gray-band, Phase-1/Phase-2 isolation, equivalent-reference reuse, exhausted-budget, exclusion, and once-only Mooncake-reference branch.

## 7. Run Focused Preflight Verification

- [x] 7.1 Run the focused configuration, protocol, lifecycle, scheduling, connector, reset, metrics, launcher, and report unit tests through the target `.venv/bin/python -m pytest`; verify all selected tests pass and retain the exact command/output artifact.
- [x] 7.2 Run changed-file format, lint, and type checks through the repo-local environment and pre-commit hooks; verify all applicable checks pass without unrelated formatting churn.
- [ ] 7.3 Run a read-only hardware/runtime preflight for both A800s, NUMA affinity, model readability, dataset checksum, CPU offload capacity, target/source imports, Mooncake 0.3.8 import, and free ports; verify blockers are reported before any timed case and do not create smoke performance evidence.

## 8. Execute Phase-1 End-To-End Acceptance

- [ ] 8.1 Launch GPU0 native v0.22 baseline and GPU1 target agent-only queues concurrently at QPS `1.0`, `0.5`, then `0.1`, with a fresh server for every case; verify all six full 24-DAG cases qualify and persist complete artifacts.
- [ ] 8.2 After GPU0 baseline, run target offload-agent Phase 1 on GPU0 at QPS `1.0`, `0.5`, then `0.1` while the independent GPU1 queue proceeds or becomes idle naturally; verify all three cases qualify, record the actual peer/idle state, and retain native/TokenCake transfer and scheduling metrics.
- [ ] 8.3 Apply the accepted failure/gray-band rule to the native and agent-only target comparisons and run only the required additional pair members; verify no mode/QPS receives more than three total launches including exclusions, strict gates use qualifying medians, and an exhausted non-qualifying budget remains unresolved.
- [ ] 8.4 After all target primary cases complete, start the unchanged latest-old offload-agent queue on GPU0 and the current Mooncake queue on GPU1 concurrently, each at QPS `1.0`, `0.5`, then `0.1`; verify the old results record `FINISHED_PREEMPTED` and Mooncake runs the full workload without connector errors.
- [ ] 8.5 Exclude work-inequivalent old results from the parity gate without inventing an alternate comparison; for each work-equivalent target-versus-old comparison, apply the failure/gray-band rule after both initial reference queues finish, reuse existing target repeats, and run only missing pair members up to three total launches per mode/QPS including exclusions.
- [ ] 8.6 Report each correctness-qualified Mooncake timing as a once-per-QPS non-gating speed reference and do not repeat Mooncake for a hard performance decision.
- [ ] 8.7 Produce the Phase-1 per-QPS gate and attribution report using `performance.total_e2e_s`; verify it evaluates native improvement of at least 10%, target agent-only regression no greater than 5%, and qualifying latest-old parity, with diagnostic latency/throughput/retry/transfer data kept secondary.

## 9. Apply The Evidence-Gated Phase 2 Only If Needed

- [ ] 9.1 From failed Phase-1 gate metrics, either document that snapshot-known candidate loss/overwrite timing is not the cause and leave Phase 2 absent, or demonstrate that a still-active ordered snapshot contained useful blocks that were lost before earlier preservation or reached the exact imminent-overwrite window; report snapshot-external frontier evidence separately because it cannot trigger this minimal Phase 2.
- [ ] 9.2 If and only if task 9.1 attributes a required gate miss to that snapshot-known timing gap, reuse the Phase-1 read-only frontier observation at the native pre-allocation point, intersect exact imminent-overwrite entries with ordered active snapshots, require continuous CPU/GPU-reachable snapshot ancestry, and retain native connector D2H-before-overwrite ordering; otherwise mark this task not triggered, and verify native disabled/offload-only tests remain unchanged.
- [ ] 9.3 If Phase 2 is implemented, rerun focused block-pool/connector/scheduler tests and only the affected target comparison pairs under the same A800 contract; give each affected Phase-2 target-offload-agent mode/QPS its own at-most-three-launch budget, never pool it with Phase 1, and reuse a prior reference only after recording that its relevant executable behavior and inputs are unchanged; otherwise record that no rerun is required, and verify the final gate inputs reference the correct implementation phase and artifact hash.
- [ ] 9.4 If a hard gate still fails and evidence attributes the miss to hot frontier blocks absent from every retained snapshot, stop and request an explicit scope decision before adding any bounded global lineage/reuse/ineffective-preservation tracker.

## 10. Final Review And Handoff

- [ ] 10.1 Re-run strict OpenSpec validation, `git diff --check`, focused tests, and applicable pre-commit hooks; verify all commands and results are recorded and no implementation task relies on a smoke run.
- [ ] 10.2 Review every changed line against the minimal-scope exclusions, vLLM license/style conventions, disabled native behavior, source-worktree cleanliness, and human-review requirements; verify the handoff explicitly identifies AI assistance and any remaining unvalidated TP/PP scope.
- [ ] 10.3 Deliver the final correctness/performance/provenance report and implementation diff for human review; verify every hard gate has a qualifying result or an explicit user-owned unresolved decision, and do not open a PR until the mandatory duplicate-work checks and human review are completed.
