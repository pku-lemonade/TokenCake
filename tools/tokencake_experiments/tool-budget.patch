diff --git a/agent/app/code_writer_paper_pressure.py b/agent/app/code_writer_paper_pressure.py
--- a/agent/app/code_writer_paper_pressure.py
+++ b/agent/app/code_writer_paper_pressure.py
@@ -3,3 +3,3 @@
 import subprocess
-from dataclasses import dataclass
+from dataclasses import dataclass, replace
 from pathlib import Path
@@ -259,6 +260,14 @@ def _configure_conversation(graph: InDegreePriorityGraph) -> None:
     for node in graph.nodes.values():
         if not isinstance(node, LLMAppNode):
             continue
+        if isinstance(node, (
+            McpSearchNode, McpFileWriteNode, McpConditionalExecutionNode,
+            McpExternalToolEvalNode, McpUserConfirmNode, McpExternalTestNode,
+        )) and node.kvargs.get("stage_type") not in (
+            "branch_validation", "revision_validation",
+        ):
+            node.metadata = replace(node.metadata, max_new_tokens=128)
         predecessors = sorted(graph.predecessors(node.uuid),
@@ -316,5 +324,6 @@ class CodeWriterPaperPressureApplication(LLMApplication):
     profile_name = PAPER_PRESSURE_PROFILE
     input_composition = "append-only-conversation-v1"
+    tool_instruction_max_tokens = 128
 
diff --git a/vllm_serving.py b/vllm_serving.py
--- a/vllm_serving.py
+++ b/vllm_serving.py
@@ -190,5 +190,8 @@ def generate_requests_app(
             app_request.frozen_workload_contract["input_composition"] = (
                 application.input_composition)
+        if hasattr(application, "tool_instruction_max_tokens"):
+            app_request.frozen_workload_contract["tool_instruction_max_tokens"] = (
+                application.tool_instruction_max_tokens)
         requests.append(app_request)
